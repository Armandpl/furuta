# @package _global_
defaults:
  - override /env: sim
  - override /wrappers: base_wrappers

gym_id: "FurutaSim-v0"
total_timesteps: 100_000
evaluation:
  early_stopping_reward_threshold: null
model_artifact: "model:v20"

# @package _global_
defaults:
  - override /env: real
  - override /wrappers: real_wrappers
  - override /algo: sac_real

total_timesteps: 100_000
n_envs: 1
model_artifact: "model:v61"
capture_video: False

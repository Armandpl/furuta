# @package _global_
defaults:
  - override /env: sim
  - override /wrappers: base_wrappers
  - override /algo: sac

# n_envs: 1
gym_id: "FurutaSim-v0"
total_timesteps: 200_000
evaluation:
  early_stopping_reward_threshold: null
  eval_freq: null # no eval no early stopping

# @package _global_
defaults:
  - override /env: sim
  - override /wrappers: base_wrappers
  - override /algo: sac

n_envs: 12
gym_id: "FurutaSim-v0"
total_timesteps: 150_000
evaluation:
  early_stopping_reward_threshold: null

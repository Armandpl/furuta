program: train.py
method: grid
parameters:
  env:
    values: ["sim", "sim_encoders", "sim_precise_encoders"]
  env.velocity_filter:
    values: [null, 2, 3, 4, 10]
  seed:
    values: [1, 2, 3, 4]

command:
  - ${env}
  - python
  - ${program}
  - ${args_no_hyphens}
  - +experiment=sim
